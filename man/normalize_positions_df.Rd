% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_position.R
\name{normalize_positions_df}
\alias{normalize_positions_df}
\title{Normalize Positions in Data Frame}
\usage{
normalize_positions_df(
  df,
  position_col = "position",
  genome_lengths = NULL,
  reference_file = NULL,
  seq_id_col = NULL
)
}
\arguments{
\item{df}{Data frame with position data}

\item{position_col}{Name of position column(s)}

\item{genome_lengths}{Named vector of genome lengths (names = seq_id), OR}

\item{reference_file}{Path to reference file (.gbk, .fasta, .dna) to auto-parse lengths}

\item{seq_id_col}{Column containing sequence ID (auto-detected if NULL)}
}
\value{
Data frame with normalized positions
}
\description{
Applies position normalization to an entire data frame, handling
multiple replicons (chromosome + plasmids) if present.
Can auto-detect genome lengths from reference file.
}
\examples{
\dontrun{
# Auto-detect from GenBank (recommended)
df <- normalize_positions_df(df, reference_file = "reference.gbk")

# Or provide lengths manually
df <- normalize_positions_df(df, genome_lengths = c(chromosome = 4600000, pBR322 = 4361))
}
}
