% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_named_tables.R
\name{compare_named_tables}
\alias{compare_named_tables}
\title{Compare Named Tables from Two Lists}
\usage{
compare_named_tables(list_A, list_B, name_A = NULL, name_B = NULL)
}
\arguments{
\item{list_A}{A named list of data frames, representing the first set of tables (e.g., control data).}

\item{list_B}{A named list of data frames, representing the second set of tables (e.g., experimental data).}
}
\value{
A named list of data frames containing the comparison results for each table.
Each data frame includes columns from both input tables and a \code{status} column indicating:
- "New in B": Rows present in \code{list_B} but missing in \code{list_A}.
- "Missing in B": Rows present in \code{list_A} but missing in \code{list_B}.
- "Unchanged or Modified": Rows that exist in both but may have differences in other columns.
}
\description{
This function compares named tables from two lists (e.g., experimental and control datasets).
It identifies differences based on a specified column ("position" or "start") depending on the table name.
}
\examples{
# Example usage:
comparison_results <- compare_named_tables(list_A, list_B)
for (name in names(comparison_results)) {
  print(comparison_results[[name]])
}
}
